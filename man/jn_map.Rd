% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jn_map.R
\name{jn_map}
\alias{jn_map}
\title{Two-dimensional Johnson-Neyman map of a conditional slope}
\usage{
jn_map(formula, model, ci = 0.95, n_grid = 100, ...)
}
\arguments{
\item{formula}{an object of class \code{\link{formula}} of the form
\code{outcome ~ focal | m1 + m2}. Exactly two bare moderators are required;
additional moderators present in the SIMPLE statement must be pinned via
\code{\link[=at]{at()}}.}

\item{model}{an \code{\link{blimp_obj}}. The model must have a SIMPLE command output
saved.}

\item{ci}{a value between 0 and 1 specifying the credible-interval size for
the significance contour. Default \code{0.95}.}

\item{n_grid}{integer length-1 or length-2. Number of grid points along
\code{m1} and \code{m2}. Default \code{100} (i.e. 100 x 100 cells).}

\item{...}{currently unused.}
}
\value{
a \code{\link[ggplot2:ggplot]{ggplot2::ggplot}} plot. Attribute \code{"grid"} carries the per-cell
data frame (with \code{lower}, \code{median}, \code{upper}, \code{sig} columns).
}
\description{
A 2-D analogue of \code{\link[=jn_plot]{jn_plot()}}. For a model with an \verb{outcome ~ focal x m1 x m2}
interaction, the conditional slope of \code{outcome} on \code{focal} is a linear
surface over \verb{(m1, m2)}:
\deqn{ \frac{\partial y}{\partial x} = a + b\,m_1 + c\,m_2 + d\,m_1 m_2. }
\code{jn_map()} recovers those four coefficients per MCMC draw from the
SIMPLE-evaluated points, evaluates them on a dense grid, and renders the
posterior median as a heatmap. A contour line marks the boundary of the
region where the \code{ci} posterior credible interval excludes zero -- the 2-D
"region of significance".
}
\examples{
\dontshow{if (has_blimp()) withAutoprint(\{ # examplesIf}
\dontrun{
# Generate data with a three-way interaction
mydata <- rblimp_sim(
    c(
        'x  ~ normal(0, 1)',
        'm1 ~ normal(0, 1)',
        'm2 ~ normal(0, 1)',
        'y  ~ normal(10 + 0.4*x + 0.3*m1 - 0.2*m2 +
                         0.5*x*m1 + 0.3*x*m2 + 0.2*m1*m2 +
                         0.6*x*m1*m2, 1)'
    ),
    n = 500,
    seed = 981273
)

# Fit the model -- SIMPLE must evaluate both moderators
fit <- rblimp(
    'y ~ x m1 m2 x*m1 x*m2 m1*m2 x*m1*m2',
    mydata,
    center = ~ x + m1 + m2,
    simple = 'x | m1 @ quantile and m2 @ sd',
    seed   = 1071,
    burn   = 1000,
    iter   = 1000
)

# Default 2-D heatmap of the conditional slope of y on x
jn_map(y ~ x | m1 + m2, fit)

# Swap axes
jn_map(y ~ x | m2 + m1, fit)

# Inspect the underlying per-cell data
head(attr(jn_map(y ~ x | m1 + m2, fit), 'grid'))
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{jn_plot}}, \code{\link{simple_plot}}, \code{\link{at}}, \code{\link{join}}
}
