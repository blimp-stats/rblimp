% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{at}
\alias{at}
\title{Marker for fixing a moderator at specific SIMPLE evaluated values}
\usage{
at(...)
}
\arguments{
\item{...}{named arguments where each name is a moderator in the SIMPLE
command and each value is a single label or a vector of labels
(e.g., \code{"Q25"}, \code{"+1 SD"}, \code{"0"}, or a parameter name) to keep.}
}
\value{
Calling \code{at()} outside a formula raises an error; the function only
has meaning when it appears in a \code{simple_plot()} or \code{jn_plot()} formula.
}
\description{
Used inside \code{\link{simple_plot}} and \code{\link{jn_plot}} formulas to restrict the SIMPLE
rows to those whose moderator was evaluated at the given label(s).
}
\details{
If \code{at()} is the only term after \code{|}, the moderators it names are also the
ones plotted, so \code{y ~ x | at(m = c("-1 SD", "+1 SD"))} shows just those two
values of \code{m} without having to name \code{m} twice.
}
\examples{
\dontrun{
# Pin `m2` at the SIMPLE label "0", leaving `m1` to color the lines
simple_plot(y ~ x | m1 + at(m2 = "0"), model)

# Pin to a subset (vector of labels) -- `m2` still faces over those values
simple_plot(y ~ x | m1 + at(m2 = c("Q25", "Q75")), model)

# Show only some of a single moderator's values (no bare name needed)
simple_plot(y ~ x | at(m1 = c("Q25", "Q50", "Q75")), model)

# Use inside `jn_plot()` the same way
jn_plot(y ~ x | m1 + at(m2 = "+1 SD"), model)

# In a 4-way fit, pin one moderator and map the slope surface over the other two
jn_map(y ~ x | m1 + m2 + at(m3 = "0"), four_way_model)
}
}
\seealso{
\code{\link{simple_plot}}, \code{\link{jn_plot}}, \code{\link{jn_map}}, \code{\link{join}}
}
