% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{join}
\alias{join}
\title{Marker for bundling multiple SIMPLE moderators into one compound moderator}
\usage{
join(...)
}
\arguments{
\item{...}{bare moderator names (at least two) to bundle together.}
}
\value{
Calling \code{join()} outside a formula raises an error; it only has
meaning inside \code{simple_plot()} / \code{jn_plot()} formulas.
}
\description{
Used inside \code{\link{simple_plot}} and \code{\link{jn_plot}} formulas to treat a set of
SIMPLE moderators as a single conceptual moderator. Each row's color/legend
label combines every component's \code{"name @ value"} so that, e.g., the dummy
codes of a nominal predictor render as one set of colored lines instead of
a sparse facet grid.
}
\details{
Only the first bare term on the right-hand side of \code{|} may use \code{join(...)}
(that is, the color / x-axis position). Facet positions must remain plain
moderator names or \code{at(...)} calls.
}
\examples{
\dontrun{
# Nominal predictor with dummy codes -- bundle them as one moderator
simple_plot(y ~ x | join(group.1, group.2), model)

# Combine with `at()` to pin an unrelated moderator
simple_plot(y ~ x | join(group.1, group.2) + at(z = "0"), model)

# In a 4-way fit, collapse the two facet moderators so the bound
#   annotation goes back into each strip of the JN plot
jn_plot(y ~ x | m1 + join(m2, m3), four_way_model)
}
}
\seealso{
\code{\link{simple_plot}}, \code{\link{jn_plot}}, \code{\link{jn_map}}, \code{\link{at}}
}
