% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple_effects.R
\name{simple_effects}
\alias{simple_effects}
\title{Posterior summaries of conditional (simple) effects}
\usage{
simple_effects(model, ci = 0.95, digits = 3)
}
\arguments{
\item{model}{a \code{\link{blimp_obj}}. The model must have a \code{SIMPLE} command output
saved.}

\item{ci}{a value between 0 and 1 specifying the credible-interval size.
Default \code{0.95}.}

\item{digits}{integer number of decimal places used when the result is
printed. Default \code{3}.}
}
\value{
invisibly, an object of class \code{blimp_simple_effects}: a named list
with one numeric matrix per conditional effect (rows are \code{Intercept}/\code{Slope}
or \code{Effect}; columns are the posterior summaries). Returns \code{NULL}
(invisibly) with a message when no \code{SIMPLE} output is saved.
}
\description{
Summarizes the conditional-effect posterior draws produced by Blimp's
\code{SIMPLE} command (stored in \code{model@simple}) as a table of posterior
medians, standard deviations, and credible-interval bounds. This surfaces
simple slopes numerically -- the same effects that \code{\link{simple_plot}},
\code{\link{jn_plot}}, and \code{\link{jn_map}} visualize -- which are otherwise not shown by
\code{\link{summary}}. When \code{model@simple} is non-empty, the same table is also
appended to \code{summary(model)} automatically.

Three kinds of conditional effect are reported, one block per effect:
\describe{
\item{Conditional intercept and slope}{From \verb{INTER:}/\verb{SLOPE:} draws, e.g.
\verb{y ~ x | m @ +1 SD}. Each block has an \code{Intercept} and a \code{Slope} row.}
\item{Compound-parameter effect}{From \verb{PARAM:} draws -- a generated
conditional effect whose focal is a labeled-parameter expression (e.g.
\code{b4 + b7*mod}). Each block has a single \code{Effect} row (no intercept).}
}
}
\examples{
\dontshow{if (has_blimp()) withAutoprint(\{ # examplesIf}
\dontrun{
mydata <- rblimp_sim(
    c(
        'x ~ normal(0, 1)',
        'm ~ normal(0, 1)',
        'y ~ normal(10 + 0.5*x + m + 0.2*x*m, 1)'
    ),
    n = 200,
    seed = 981273
)
m1 <- rblimp(
    'y ~ x m x*m',
    mydata,
    center = ~ m,
    simple = 'x | m',
    seed = 10972,
    burn = 1000,
    iter = 1000
)
simple_effects(m1)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{simple_plot}}, \code{\link{jn_plot}}, \code{\link{jn_map}}, \code{\link{summary}}
}
